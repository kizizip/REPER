spring:
  server:
    port: 48620
  config:
    import: optional:application-secrets.yml
  datasource:
    url: jdbc:mysql://mysql:13790/reper?&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ssafy
    password: aoreoya5!^^
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # (개발환경) -> (운영 환경) validation/none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging.level:
  org.hibernate.SQL: debug # (개발 환경) SQL 로그 출력
  org.hibernate.orm.jdbc.bind: trace # (개발 환경) SQL 바인딩 값 출력

springdoc.override-with-generic-response: false

kakao:
  client_id: 11a435ceceba6f2724e9f7b1b9b69f5b
  redirect_uri: http://localhost:8080/api/auth/callback